#!/usr/bin/env bash

# npm install &&
# cpanm --notest --local-lib extlocal --no-man-pages --install LWP::UserAgent XML::Parser HTML::Entities XML::DOM RTF::HTMLConverter &&
serverless -s dev offline start
